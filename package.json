{
  "name": "testnet-subql-uniqueone",
  "version": "1.0",
  "description": "This project can be use as a starting po...",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "subql-publish": "./scripts/build.sh && subql publish",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Octopus Team",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^9",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
